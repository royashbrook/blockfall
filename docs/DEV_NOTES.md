# Development Notes

- In-game screenshots are written to `~/blockfall-shots` (`/Users/roy/blockfall-shots` on Roy's local machine). The renderer owns this path in `app/Sources/BlockfallApp/Renderer.swift`.

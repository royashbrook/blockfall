# 2026-06-29 Claude Crash Recovery

Source transcript:

- `/Users/roy/.claude/projects/-Users-roy-gh-blockfall/a4484b8e-805d-4ab4-ab63-86a0bbbb5d14.jsonl`

Current base:

- Main checkout started clean at `f6bd408`.
- `git status --short --branch` showed `## main...origin/main`.
- Open issues to finish: #146, #147, #148, #149, #150, #151.

Recovered state:

- Roy confirmed #138 water good, #142 torch holes closed, #143 trees-in-buildings fixed, #136 god-ray amount right, #147 residual grain remains.
- Clouds are already toggleable with the pause-menu `Volumetric Clouds` checkbox.
- Claude filed #146-#151 and launched four isolated agents before the spend limit wall.
- Agent worktrees are still present under `.claude/worktrees/` at base `f6bd408`.

Agent outputs:

- `agent-affef0d7485f0c090`: `Renderer.swift` only. Handles #146 cloud ripple and #147 god-ray grain. Release build passed in the worktree, but the agent hit the spend limit before screenshots/full gate.
- `agent-a9009030355b99b7d`: `worldgen_part3.rs` and `worldgen_part4.rs`. Handles #148/#149 door and lintel bugs. The added test was still failing/running at the spend limit; review carefully before trusting.
- `agent-a48db92d28a45de1d`: `EntityRenderer.swift` only. Handles #150 animal shadows on snow. Built and captured after-fix screenshots, but hit the spend limit before final gate.
- `agent-a430dc967100595cc`: `mesher.rs` only. Handles #151 floating snow. Snow mesher tests passed and `./ci/check.sh` was GREEN in the worktree.

Next actions:

- Inspect each agent diff and port only the parts that survive review into the main checkout.
- Run focused tests for the worldgen/mesher lanes, then full `./ci/check.sh`.
- Visual fixes need screenshot/play verification where possible before closing issues.
- Close issues only after the main checkout is verified, with concise comments.

## 2026-06-29 Playtest Follow-Up

Roy retested `4f239a1` and reported:

- startup was very slow on a desert spawn, with the desert packed in ground cover.
- #146 cloud ripple/scaly artifacts still appeared when flying creative through/under terrain. Evidence screenshot: `/Users/roy/blockfall-shots/shot_20260629_153638_039_x-20y13z117_SE_night_desert.png`.
- #147 god-ray grain looked basically unchanged.
- #148 still appeared in a snowy building, with only the top half opening. Evidence screenshot: `/Users/roy/blockfall-shots/shot_20260629_153811_401_x-117y39z-219_W_night_snowy.png`.
- #149 was not seen; keep closed unless new evidence appears.
- #150 looked fixed.
- #151 was not found.

Issue state after intake:

- #146, #147, #148 reopened.
- #152 created for desert startup / no desert mushrooms / larger varied cactus.
- #149, #150, #151 remain closed.

Patch state on `codex/roy-bf-blockfall`:

- #146: sky pass now fades volumetric clouds out quickly as `frame.camera.underground` rises, so creative underground / inside-terrain views should not keep raymarching visible cloud strata through the cave wash.
- #147: god-ray start jitter is now a small centered offset instead of a full-step random shift, and the shaped shaft signal gets an additional 2x2 quad average. Needs Roy eyeballs; headless harness could not reproduce the exact visual complaint.
- #148: door interaction and mining now canonicalize the whole contiguous vertical door run. A top-half interaction cannot leave top and bottom halves in different open/closed states.
- #152: desert generation no longer places mushrooms on sand. Desert cactus placement is sparser, and cactus props render as seed-varied tall silhouettes (about 2x to 5x the old height, with optional arms) without changing collision/block storage.

Verification:

- `cargo test --manifest-path engine-rs/bfcore/Cargo.toml desert_columns_do_not_generate_mushrooms -- --nocapture` passed.
- `cargo test --manifest-path engine-rs/bfcore/Cargo.toml doors_open_close -- --nocapture` passed.
- `cargo test --manifest-path engine-rs/bfcore/Cargo.toml structure_doors_are_single_2tall_with_solid_lintel -- --nocapture` passed.
- `./ci/build.sh release` passed.
- Headless Metal shots wrote to `/private/tmp/blockfall-steady-kepler-20260629/`, but they spawned/framed enclosed terrain and were not useful visual proof for desert/cactus or the exact underground-cloud case.
- `./ci/check.sh` passed GREEN.

Next visual ask:

- Retest #146 by entering creative and flying underground / through terrain with volumetric clouds on.
- Retest #147 near the sun with god rays enabled.
- Retest #148 by opening generated structure doors from both top and bottom halves.
- Retest #152 on a fresh desert spawn: no mushrooms, sparse rocks/stones, cactus visibly taller and varied.

## 2026-06-29 Second Playtest Intake

Roy retested after `6d4aeaf` and reported:

- #153: New ocean artifact: large blue lines/planes in ocean water, likely on chunk lines. Evidence screenshot: `/Users/roy/blockfall-shots/shot_20260629_160509_302_x-8y9z25_S_night_snowy.png`.
- #146 cloud ripple still happens after running around; underground/creative is not the sole trigger.
- #147 god-ray grain is still basically unchanged.
- #148 is narrowed to isolated generated houses, not cities. The top and bottom door halves render opposite open/closed states; clicking swaps the visual mismatch, and collision/open state can disagree with one visual half.
- #152 desert/cactus work is fixed by Roy's retest.
- #154: New streaming/perf issue: reaching the boundary of the initially loaded area can freeze the whole game for roughly a minute, then return to normal FPS once generation/meshing catches up.

Patch state after this intake:

- #153: water faces no longer emit against a missing neighbor chunk in the mesher snapshot. This should prevent transparent blue chunk-edge slabs while adjacent ocean chunks are still streaming.
- #148: door mesh orientation is now chosen from the entire contiguous vertical door run, so top and bottom halves cannot infer different hinge axes from different nearby wall/window blocks.
- #154: post-startup catch-up now uses moderate generation/mesh/upload budgets. Startup-class bulk fill is limited to early resident-set loading, reducing the chance of a large boundary backlog becoming a frame-thread lighting/upload burst during exploration.

Still open after this patch:

- #146 needs a better repro/visual fix; current underground fade did not catch the running-around case.
- #147 needs a stronger god-ray grain fix; the shader-side smoothing was not enough.
- Ocean water and boundary streaming need Roy eyeballs after gate.

## 2026-06-29 Third Playtest Intake

Roy retested after `771fdf4` and reported:

- #146 is definitely the volumetric cloud path: toggling `Volumetric Clouds` off makes the ripple/scaly artifact disappear.
- #147 god-ray grain is still present.
- #148 still happens in non-city houses. Evidence screenshot: `/Users/roy/blockfall-shots/shot_20260629_184831_090_x97y15z-333_E_night_plains.png`.
- #153 ocean blue chunk-line planes are improved but not gone; some resolve eventually, some persist.
- #154 overland boundary streaming is much improved, but reaching an ocean can still briefly choke before snapping back to normal.

Patch state after this intake:

- #146: volumetric clouds now fade out at low/grazing ray angles more aggressively, use more raymarch samples, lower opacity, and much smaller per-ray jitter. This targets the visible step/ripple pattern in the cloud march while keeping the toggle and high-sky puffs.
- #148: mixed open/closed vertical door runs now render from the bottom half's canonical state, and interaction toggles from that same bottom-half state no matter which half is targeted.
- #153: streaming now marks a newly resident chunk's already-resident face-neighbours dirty too, so meshes that depended on an unknown neighbour can rebuild when the neighbour arrives.
- #154: deep open-ocean underwater vegetation is sparser and shorter to reduce ocean-entry generation/prop/mesh pressure; shallow/freshwater decoration is unchanged.
- #147 remains open for a separate god-ray composite pass.

## 2026-06-29 Fourth Playtest Intake

Roy retested after `1bb76c7` and reported:

- #146 still recurred. Trigger is unclear: possibly pause, flying through things, or render lag. Volumetric clouds remain implicated because toggling them off removes it.
- #148 looked fixed across a couple of checked non-city houses; keep open until it survives more incidental play.
- #153 is specifically tied to water chunks streaming in: starting near water can look fine, but walking away across ocean/lake water exposes blue grid lines on chunk boundaries.
- #154 water traversal is less bad, but the chunk blue lines remain the visible water-streaming problem.

Patch state after this intake:

- #153/#154: water meshing now emits only top water surfaces, not vertical side/bottom water faces. This removes the blue vertical water walls that can appear when chunk-neighbour state is transitional, and reduces water mesh work.
- #146 remains open for a focused volumetric-cloud repro/fix.

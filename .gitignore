# Build outputs
/build/
/dist/
app/.build/
*.dmg

# Copied from /contract by ci/build.sh (single source of truth is /contract)
app/Sources/CBlockcore/include/engine_c_api.h

# Local saves / scratch
*.blockfall_save/
.DS_Store

# In-game screenshots (backslash key). Saved to ~/blockfall-shots (outside the
# repo), but ignore any in-repo copies/symlinks too so shots are never committed.
blockfall-shots/
shots/
shot_*.png
/graphify-out/

# Python
__pycache__/
*.pyc
.claude/

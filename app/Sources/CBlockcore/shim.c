/* Empty translation unit. CBlockcore is effectively a header-only interop
 * module over the frozen C ABI (include/engine_c_api.h, copied from /contract
 * by ci/build.sh). SwiftPM requires at least one source file in a C target. */
#include "engine_c_api.h"
